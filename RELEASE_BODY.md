# بازارسنج ۳.۲.۰ — بسته پرتابل رسمی + رفع ارور node.exe + اسکن کامل کافه‌کالا

## 📦 دانلود — دو گزینه

| فایل | مناسب برای | پیش‌نیاز |
|---|---|---|
| **BazarSanj-3.2.0-Portable-win-x64.zip** (≈۲۵۵MB) | اکثر کاربران — **بدون هیچ نصبی**؛ node.exe و کرومیوم داخل بسته | هیچ (ویندوز ۱۰/۱۱ x64) |
| **TorobScanner_3.2.0_win-x64.zip** (سبک) | کاربرانی که .NET دارند | .NET 9 Desktop Runtime + مرورگر |

### اجرای نسخه‌ی پرتابل
1. Extract در پوشه‌ای با مسیر انگلیسی (مثلاً `C:\BazarSanj`)
2. اگر ویندوز ZIP را بلاک کرد: قبل از Extract → راست‌کلیک روی ZIP → Properties → تیک **Unblock**
3. `BazarSanj.exe` را اجرا کنید — بدون نصب .NET، بدون Node.js، بدون مرورگر

## 🐛 رفع اشکال
- **ارور node.exe هنگام اسکن** — ریشه: درایور Playwright انتظار `node.exe` کنار DLL را دارد و نبودنش اسکن را کلاً متوقف می‌کرد. رفع: node.exe + درایور + کرومیوم headless-shell همگی داخل بسته‌ی پرتابل باندل شدند + پیام راهنمای فارسی سه‌مرحله‌ای در صورت قرنطینه‌ی آنتی‌ویروس
- **کافه‌کالا فقط ~۱۵ محصول شناسایی می‌کرد** — ریشه: Rank Math آدرس صفحه‌ی ۲ را ۳۰۱ می‌کرد و دکمه‌ی AJAX «بارگیری بیشتر» دنبال نمی‌شد. رفع: کلیک هوشمند LoadMore + شمارش پول کارت‌ها + محافظ ریدایرکت → **۱۲۳ محصول یونیک**
- سرعت اسکن LoadMore حدود **۵ برابر** (۱۲۳ محصول در ~۳۴ ثانیه به‌جای ~۳ دقیقه)

## ✨ جدید در ۳.۱/۳.۲
- جستجوی زنده‌ی محصولات با نرمال‌سازی فارسی (ی/ک عربی، نیم‌فاصله)
- فیلتر فروش‌رفته/ناموجود سه‌لایه (JSON-LD → Microdata → برچسب DOM)
- تم Platinum-Glass + فونت Vazirmatn باندل‌شده
- README و مجوز MIT برای نسخه‌ی اپن‌سورس

## 📋 تغییرات کامل
ریشه‌یابی همه‌ی باگ‌ها: فایل [BUGFIX-CHANGELOG.md](https://github.com/ArtinSJ/Price-Scanner/blob/main/BUGFIX-CHANGELOG.md) در مخزن.
