# بازارسنج ۳.۵.۱ — اصلاح نمایش نسخه در نوار عنوان (چیپ نسخه حالا همیشه دقیق است)

## 📦 دانلود — دو گزینه

| فایل | مناسب برای | پیش‌نیاز |
|---|---|---|
| **BazarSanj-3.5.1-Portable-win-x64.zip** (≈۲۵۵MB) | اکثر کاربران — **بدون هیچ نصبی**؛ node.exe و کرومیوم داخل بسته | هیچ (ویندوز ۱۰/۱۱ x64) |
| **BazarSanj-3.5.1-Lite-win-x64.zip** (≈۴۳MB) | کاربران حرفه‌ای — درایور node.exe داخل بسته است | .NET 9 Desktop Runtime + مرورگر سیستم (Edge/Chrome) |

### اجرای نسخه‌ی پرتابل
1. Extract در پوشه‌ای با مسیر انگلیسی (مثلاً `C:\BazarSanj`)
2. اگر ویندوز ZIP را بلاک کرد: قبل از Extract → راست‌کلیک روی ZIP → Properties → تیک **Unblock**
3. `TorobScanner.exe` را اجرا کنید — بدون نصب .NET، بدون Node.js، بدون مرورگر

### 🔄 آپدیت از ۳.۵.۰ یا ۳.۲.۲ — دستی دانلود نکنید!
فقط برنامه را باز کنید و **بررسی بروزرسانی** را بزنید:
1. پنجره‌ی بروزرسانی نسخه‌ی ۳.۵.۱ را پیدا می‌کند و بسته‌ی هم‌نوع نصب شما را دانلود می‌کند (پرتابل → بسته‌ی پرتابل، سبک → بسته‌ی سبک)
2. برنامه بسته می‌شود، فایل‌ها جایگزین می‌شوند و **خودش دوباره باز می‌شود** — روی ۳.۵.۱
3. بعد از آپدیت، چیپ کنار لوگوی «BAZARSANJ» باید دقیقاً **۳.۵.۱** را نشان دهد — همان چیزی که این نسخه درستش کرده ✓

> اگر نسخه‌ای قدیمی‌تر از ۳.۲.۲ دارید، اول ۳.۲.۲ را یک بار دستی نصب کنید (آپدیتر نسخه‌های قبل از ۳.۲.۲ همان کد قدیمی و معیوب است).

## 🔐 انسجام داده‌ها
**بسته‌ی پرتابل (≈۲۵۵MB):**
  - MD5: `8d485598854356bb9b230bfce8986e6a`
  - SHA-256: `e19e99dd08dbf4c6d217adc0e36efaa9bc0e6e7eca0dc69ca1085c8a8b93d81d`
  - ۸۱۶ فایل — ساختار بایت‌به‌بایت یکسان با ۳.۵.۰ به‌علاوه‌ی ۳ فایل برنامه + راهنمای به‌روزشده

**بسته‌ی سبک Lite (≈۴۳MB):**
  - MD5: `15ca9002aa0245fd5725de529666aa77`
  - SHA-256: `cc4b16ff74c47f62d0e9183517592c01448563b60f286808a680d33a3f391f37`

## 🐛 باگ ۴۱ — چیپ نسخه در نوار عنوان همیشه عدد قدیمی نشان می‌داد
- **مشکل:** چیپ کوچک کنار لوگوی «BAZARSANJ» در نوار عنوان، متن **هاردکد** بود («۳.۲.۰») و از نسخه‌ی ۳.۲.۰ به بعد هیچ‌وقت به‌روز نشده بود — حتی اگر خود برنامه نسخه‌ی جدیدتر بود (مثلاً ۳.۵.۰)، نوار عنوان عدد قدیمی نشان می‌داد. در نتیجه کاربر بعد از آپدیت گمان می‌کرد آپدیت اعمال نشده است
- **نمونه‌ی دوم همین کلاس باگ:** فایل `بخوانید.txt` داخل بسته‌ی پرتابل هم هاردکد بود — هنوز «بازارسنج نسخه ۳.۲.۰» می‌گفت و کاربر را به پوشه‌ی `BazarSanj-3.2.0` راهنمایی می‌کرد. در ۳.۵.۱ متن راهنما بازنویسی شد
- **نکته‌ی ظریف:** سه پنجره‌ی دیگر (تنظیمات، مدیریت، بروزرسانی) نسخه را **پویا** از AssemblyVersion می‌خواندند و درست نشان می‌دادند — فقط همین یک چیپ در تابع `BuildTitleBar` جا مانده بود؛ یک رشته‌ی ثابت که در بازبینی‌های قبلی دیده نشد
- **رفع (MainWindow.xaml.cs):** چیپ حالا مستقیم `UpdateService.CurrentVersion()` را نمایش می‌دهد — همان منبعی که موتور بروزرسانی با تگ گیت‌هاب مقایسه می‌کند و همان منبعی که بقیه‌ی پنجره‌ها استفاده می‌کنند. از این به بعد با هر bump نسخه در csproj، نوار عنوان **به‌صورت خودکار** همگام می‌شود؛ این کلاس باگ برای همیشه حذف شد
- **نتیجه:** عدد چیپ = نسخه‌ی واقعی exe = عددی که آپدیتر می‌بیند. هیچ‌گاه دیگر تناقض ندارید

## 🧪 تضمین سلامت
- بیلد ۰ خطا / ۰ هشدار (تکرارپذیر — هش بسته‌ها در دو بیلد مستقل یکسان)
- **هیچ تغییری در موتور اسکن** (متدهای اسکنر هر سایت، BrowserLauncher، اسکریپت‌های استخراج) — تغییر فقط یک بلوک کوچک در `BuildTitleBar` + شماره نسخه csproj + راهنمای پرتابل است
- بسته‌ی پرتابل: ۸۱۶ فایل، فقط `TorobScanner.dll/exe/deps.json` + `بخوانید.txt` نسبت به ۳.۵.۰ تغییر کرده‌اند

## 📋 تغییرات کامل
ریشه‌یابی همه‌ی باگ‌ها: فایل [BUGFIX-CHANGELOG.md](https://github.com/ArtinSJ/Price-Scanner/blob/main/BUGFIX-CHANGELOG.md) در مخزن.
