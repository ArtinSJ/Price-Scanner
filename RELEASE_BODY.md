# بازارسنج ۳.۵.۰ — تثبیت زیرساخت، امنیت دیتابیس و اولین آپدیت خودکار کامل (از ۳.۲.۲)

## 📦 دانلود — دو گزینه

| فایل | مناسب برای | پیش‌نیاز |
|---|---|---|
| **BazarSanj-3.5.0-Portable-win-x64.zip** (≈۲۵۵MB) | اکثر کاربران — **بدون هیچ نصبی**؛ node.exe و کرومیوم داخل بسته | هیچ (ویندوز ۱۰/۱۱ x64) |
| **BazarSanj-3.5.0-Lite-win-x64.zip** (≈۴۳MB) | کاربران حرفه‌ای — درایور node.exe داخل بسته است | .NET 9 Desktop Runtime + مرورگر سیستم (Edge/Chrome) |

### اجرای نسخه‌ی پرتابل
1. Extract در پوشه‌ای با مسیر انگلیسی (مثلاً `C:\BazarSanj`)
2. اگر ویندوز ZIP را بلاک کرد: قبل از Extract → راست‌کلیک روی ZIP → Properties → تیک **Unblock**
3. `TorobScanner.exe` را اجرا کنید — بدون نصب .NET، بدون Node.js، بدون مرورگر

### 🔄 آپدیت از ۳.۲.۲ — این نسخه را دستی دانلود نکنید!
اگر ۳.۲.۲ دارید، فقط برنامه را باز کنید و **بررسی بروزرسانی** را بزنید:
1. پنجره‌ی بروزرسانی نسخه‌ی ۳.۵.۰ را پیدا می‌کند و بسته‌ی هم‌نوع نصب شما (پرتابل → بسته‌ی پرتابل) را دانلود می‌کند
2. برنامه بسته می‌شود، فایل‌ها جایگزین می‌شوند و **خودش دوباره باز می‌شود** — روی ۳.۵.۰
3. این اولین تست کامل مسیر آپدیت درون‌برنامه‌ای است ✓

> اگر نسخه‌ای قدیمی‌تر از ۳.۲.۲ دارید، اول ۳.۲.۲ را یک بار دستی نصب کنید (آپدیتر نسخه‌های قبل از ۳.۲.۲ همان کد قدیمی و معیوب است).

## 🔐 انسجام داده‌ها
- **بسته شدن فایل‌های zip از اینترنت؟** — صحت بسته‌ی پرتابل:
  - MD5: `c00e19a90b441c4342f9bb679f435ad4`
  - SHA-256: `64b3eeeddef88bfc7f33ff4380859378852960e95b92c6457186c3639191ad73`
  - ۸۱۶ فایل — ساختار بایت‌به‌بایت یکسان با ۳.۲.۲ به‌علاوه‌ی سه فایل برنامه

## ✨ جدید در ۳.۵.۰ — حاصل بازبینی کامل ۸هزار خطی سورس (اصلاحات P0)

### 🐛 باگ ۳۷ — شمارش اشتباه اعضای گروه مقایسه بعد از حذف محصول
- **ریشه:** حذف محصول فقط ردیف `SavedProducts` را پاک می‌کرد و ردیف‌های `CompareItems` (لینک‌های مقایسه) **یتیم** می‌ماندند → شمارنده‌ی گروه محصولِ حذف‌شده را می‌شمرد ولی خودش نمایش داده نمی‌شد
- **رفع:** حذف آبشاری داخل تراکنش (محصول ← لینک‌هایش) + پاک‌سازی خودکار یتیم‌های قدیمی هنگام هر استارت‌آپ (داده‌های فعلی کاربران هم ترمیم می‌شود)

### 🐛 باگ ۳۸ — اجرای همزمان دو نسخه = آسیب به دیتابیس
- **ریشه:** هیچ قفل تک‌نمونه‌ای وجود نداشت؛ دو نویسنده‌ی همزمان SQLite یعنی SQLITE_BUSY و ریسک گم‌شدن نوشته‌ها
- **رفع:** Mutex نام‌دار ویندوز — نمونه‌ی دوم با پیام فارسیِ گویا بسته می‌شود

### 🐛 باگ ۳۹ — ریشه‌کنی نهایی پروسسه‌های زامبی (علت مادری باگ ۳۶)
- **ریشه:** node.exe و کرومیوم فرزندان Playwright بودند؛ اگر برنامه وسط اسکن کرش می‌کرد یا بسته می‌شد، آن‌ها زامبی می‌ماندند و فایل‌ها را قفل می‌کردند (همان دلیلی که آپدیت شکست می‌خورد)
- **رفع:** کل برنامه در یک **Job Object** ویندوزی با `KILL_ON_JOB_CLOSE` قرار می‌گیرد — هر خروجی از برنامه (حتی کرش یا End Task از Task Manager) باعث می‌شود ویندوز همه‌ی فرزندان را خودش بکشد. دیگر هیچ زامبی‌ای اصلاً متولد نمی‌شود (اسکریپت آپدیتر به‌عنوان لایه‌ی دوم دفاعی باقی است)

### 🐛 باگ ۴۰ — لاگ خاموش در پوشه‌های فقط-خواندنی
- **ریشه:** `Logger` در پوشه‌ی برنامه می‌نوشت؛ اگر پوشه فقط-خواندنی بود (Program Files و غیره)، لاگ بی‌صدا دور ریخته می‌شد و ردپایی از خطا باقی نمی‌ماند
- **رفع:** تشخیص خودکار اولین مسیر قابل‌نوشتن (کنار exe → %APPDATA%\TorobScanner → %TEMP%) + پیام‌های خطا حالا مسیر **واقعی** لاگ را نشان می‌دهند

### ✨ قابلیت ۱۲ — خلاصه‌ی آماری اسکن و بروزرسانی
پایان هر اسکن خارجی حالا با عدد واقعی خلاصه می‌شود:
«اسکن کافه‌کالا کامل شد — ۱۲۳ جدید از ۲۵۰ محصول در ۳۴ ثانیه» — همان لحظه در نوار وضعیت و اعلان شیشه‌ای.

### ✨ قابلیت ۱۳ — خروجی CSV کنار اکسل
در پنجره‌ی خروجی، انتخاب «فایل CSV» اضافه شد: UTF-8 با BOM (سازگار با اکسل فارسی)، نقل‌قول استاندارد RFC 4180، اعداد با نقطه‌ی انگلیسی ثابت — در هر نرم‌افزار صفحه‌گسترده‌ای درست باز می‌شود.

### ⚡ بهینه‌سازی دیتابیس
`PRAGMA synchronous=NORMAL` (توصیه‌ی رسمی SQLite در کنار WAL) — سرعت نوشتن بالاتر بدون به‌خطر انداختن داده؛ در کرش برق هم WAL سالم می‌ماند.

## 🧪 تضمین سلامت
- بیلد ۰ خطا / ۰ هشدار
- **هیچ تغییری در موتور اسکن** (متدهای اسکنر هر سایت، BrowserLauncher، اسکریپت‌های استخراج) — هر ۸۱۳ فایل غیربرنامه‌ایِ بسته‌ی پرتابل بایت‌به‌بایت با ۳.۲.۲ یکسان است

## 📋 تغییرات کامل
ریشه‌یابی همه‌ی باگ‌ها: فایل [BUGFIX-CHANGELOG.md](https://github.com/ArtinSJ/Price-Scanner/blob/main/BUGFIX-CHANGELOG.md) در مخزن.
