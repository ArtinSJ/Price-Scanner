# بازارسنج ۳.۲.۱ — رفع دو باگ رابط کاربری + منوی جمع‌شونده برای مانیتورهای کوچک

## 📦 دانلود — دو گزینه

| فایل | مناسب برای | پیش‌نیاز |
|---|---|---|
| **BazarSanj-3.2.1-Portable-win-x64.zip** (≈۲۵۵MB) | اکثر کاربران — **بدون هیچ نصبی**؛ node.exe و کرومیوم داخل بسته | هیچ (ویندوز ۱۰/۱۱ x64) |
| **BazarSanj-3.2.1-Lite-win-x64.zip** (≈۴۳MB) | کاربران حرفه‌ای — درایور node.exe داخل بسته است | .NET 9 Desktop Runtime + مرورگر سیستم (Edge/Chrome) |

### اجرای نسخه‌ی پرتابل
1. Extract در پوشه‌ای با مسیر انگلیسی (مثلاً `C:\BazarSanj`)
2. اگر ویندوز ZIP را بلاک کرد: قبل از Extract → راست‌کلیک روی ZIP → Properties → تیک **Unblock**
3. `BazarSanj.exe` را اجرا کنید — بدون نصب .NET، بدون Node.js، بدون مرورگر

## ✨ جدید در ۳.۲.۱ — منوی جمع‌شونده
- **دستگیره‌ی جمع/باز کردن منوی کناری** — دایره‌ی شیشه‌ای با فلش، بالای لبه‌ی داخلی سایدبار
- با یک کلیک، منو به **ریل آیکونی باریک (~۶۶px)** جمع می‌شود و حدود **۱۸۶px فضا** به جدول محصولات آزاد می‌کند — عالی برای مانیتورهای کوچک و لپ‌تاپ
- در حالت جمع‌شده همه‌ی دکمه‌ها با همان عملکرد قبلی در دسترس‌اند (فقط آیکونی، با ToolTip راهنمای فارسی)
- انیمیشن نرم و هماهنگ با تم Platinum-Glass (محو متقابل + easing)
- وضعیت منو **ذخیره می‌شود** — دفعه‌ی بعد که برنامه باز کنید، همان‌طور که خواستید می‌ماند (settings.json)

## 🐛 رفع اشکال در ۳.۲.۱
- **سفیدشدن باکس هنگام انتخاب (ComboBox)** — ریشه: ToggleButton داخل قالب ComboBox قالب اختصاصی نداشت و ButtonChrome پیش‌فرض ویندوز هنگام hover گرادیان سفید می‌کشید. رفع: قالب شفاف اختصاصی + حالت باز شیشه‌ای پررنگ‌تر با فلش اکسنتی
- **بیرون‌زدگی نوار پیشرفت از کادر** — ریشه: در حالت نامعین، خود WPF عرض PART_Indicator را به عرض کل ترک قفل می‌کند و مقدار محلی از Setter قالب می‌چربد (رفتار تأییدشده از سورس رسمی WPF). رفع: بازنویسی کامل قالب — برش اجباری داخل PART_Track + لایه‌ی درخشش مستقل که فقط داخل محدوده حرکت می‌کند

## 📋 تغییرات کامل
ریشه‌یابی همه‌ی باگ‌ها: فایل [BUGFIX-CHANGELOG.md](https://github.com/ArtinSJ/Price-Scanner/blob/main/BUGFIX-CHANGELOG.md) در مخزن.
