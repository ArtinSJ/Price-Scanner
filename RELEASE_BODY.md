# بازارسنج ۳.۲.۲ — بازنویسی موتور بروزرسانی خودکار + منوی جمع‌شونده (از ۳.۲.۱)

## 📦 دانلود — دو گزینه

| فایل | مناسب برای | پیش‌نیاز |
|---|---|---|
| **BazarSanj-3.2.2-Portable-win-x64.zip** (≈۲۵۵MB) | اکثر کاربران — **بدون هیچ نصبی**؛ node.exe و کرومیوم داخل بسته | هیچ (ویندوز ۱۰/۱۱ x64) |
| **BazarSanj-3.2.2-Lite-win-x64.zip** (≈۴۳MB) | کاربران حرفه‌ای — درایور node.exe داخل بسته است | .NET 9 Desktop Runtime + مرورگر سیستم (Edge/Chrome) |

### اجرای نسخه‌ی پرتابل
1. Extract در پوشه‌ای با مسیر انگلیسی (مثلاً `C:\BazarSanj`)
2. اگر ویندوز ZIP را بلاک کرد: قبل از Extract → راست‌کلیک روی ZIP → Properties → تیک **Unblock**
3. `TorobScanner.exe` را اجرا کنید — بدون نصب .NET، بدون Node.js، بدون مرورگر

> ⚠️ **اگر آپدیت خودکار برایتان کار نکرده** (نسخه‌ای قدیمی‌تر از ۳.۲.۲ دارید): این نسخه را **یک بار دستی** نصب کنید — از این به بعد بروزرسانی خودکار درست کار می‌کند.

## 🔧 جدید در ۳.۲.۲ — موتور بروزرسانی خودکار بازنویسی شد
دلایل واقعی «آپدیت نمی‌شود / بعد از آپدیت باز نمی‌شود» ریشه‌یابی و رفع شد:
- **پروسسه‌های زامبی قفل فایل بودند** — node.exe و کرومیومِ باقی‌مانده از اسکن، بعد از بسته‌شدن برنامه زنده می‌ماندند و فایل‌های پوشه‌ی برنامه را قفل می‌کردند → کپی فایل‌های جدید شکست می‌خورد و اسکریپت نصب در پنجره‌ی مخفی گیر می‌کرد. رفع: قبل از کپی، پروسسه‌های اجراشده از پوشه‌ی برنامه به‌صورت هدفمند بسته می‌شوند
- **انتخاب کور بسته‌ی آپدیت** — قبلاً همیشه بسته‌ی سبک (Lite) دانلود می‌شد حتی برای نصب پرتابل. رفع: نوع نصب تشخیص داده می‌شود (hostfxr.dll) و بسته‌ی هم‌نوع دانلود می‌شود
- **شکست نصب = گم‌شدن برنامه** — اگر کپی شکست می‌خورد، اسکریپت مخفی روی pause می‌ماند و برنامه هیچ‌وقت باز نمی‌شد. رفع: خطا در `update_error.log` کنار برنامه ثبت و برنامه دوباره باز می‌شود
- **اسکریپت UTF-8 بدون BOM** + خواب امن با ping + retry دانلود (۲ بار) + timeout نیم‌ساعت + دکمه‌ی «باز کردن صفحه‌ی دانلود دستی» هنگام خطای دانلود

## ✨ از نسخه ۳.۲.۱ — منوی جمع‌شونده
- دستگیره‌ی جمع/باز کردن منوی کناری (بالای لبه‌ی داخلی سایدبار) — جمع‌شدن به ریل آیکونی ~۶۶px برای مانیتورهای کوچک + ذخیره‌ی وضعیت
- رفع سفیدشدن ComboBox هنگام انتخاب + رفع بیرون‌زدگی نوار پیشرفت

## 📋 تغییرات کامل
ریشه‌یابی همه‌ی باگ‌ها: فایل [BUGFIX-CHANGELOG.md](https://github.com/ArtinSJ/Price-Scanner/blob/main/BUGFIX-CHANGELOG.md) در مخزن.
