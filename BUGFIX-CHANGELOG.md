# 📋 گزارش تغییرات و رفع اشکال — بازارسنج | BazarSanj

**نسخه فعلی: ۳.۵.۱** — بر پایه نسخه ۲.۴.۱ اصلی

---

## 🏷️ نسخه ۳.۵.۱ — اصلاح چیپ نسخه در نوار عنوان

### 🐛 باگ ۴۱ — چیپ نسخه کنار لوگو همیشه عدد قدیمی نشان می‌داد
- **مشکل:** در `MainWindow.xaml.cs` داخل `BuildTitleBar()`، چیپ نسخه با یک رشته‌ی **هاردکد**
  ساخته می‌شد: `LuxUI.Pill("۳.۲.۰", …)`. این رشته از نسخه‌ی ۳.۲.۰ به بعد هیچ‌گاه دستی به‌روز نشد —
  بنابراین در بسته‌های ۳.۲.۱، ۳.۲.۲، ۳.۵.۰ همه نوار عنوان عدد قدیمی نشان می‌داد. نتیجه‌ی کاربردی:
  کاربر بعد از آپدیت موفق، چیپ قدیمی را می‌دید و نتیجه می‌گرفت «آپدیت اعمال نشده» (گزارش کاربر:
  «این بالا هنوز ورژن قدیمی رو نشون میده»)
- **چرا از چشم بازبینی‌ها پنهان ماند:** سه پنجره‌ی دیگر (SettingsWindow، ManagementWindow،
  UpdateWindow) نسخه را پویا از `UpdateService.CurrentVersion()` می‌خواندند و درست نشان می‌دادند؛
  فقط همین یک نقطه رشته‌ی ثابت داشت. خود چیپ هم صرفاً نمایشی است و با موتور آپدیت کاری ندارد —
  پس هیچ خطایی لاگ نمی‌شد، فقط یک عدد دروغ‌گو بود
- **نمونه‌ی دوم همین کلاس باگ:** فایل `بخوانید.txt` داخل بسته‌ی پرتابل هم هاردکد بود — هنوز
  «بازارسنج نسخه ۳.۲.۰» می‌گفت و کاربر را به پوشه‌ی `BazarSanj-3.2.0` راهنمایی می‌کرد در حالی که
  پوشه‌ی بسته‌های بعدی نام نسخه‌ی خودشان را داشت. در ۳.۵.۱ متن راهنما بازنویسی شد
- **رفع (MainWindow.xaml.cs):** رشته‌ی ثابت حذف شد و چیپ حالا از همان منبع رسمی تغذیه می‌شود:
  `LuxUI.Fa(UpdateService.CurrentVersion())` — یعنی `AssemblyVersion` واقعی اسمبلی (همان عددی که
  آپدیتر با تگ گیت‌هاب مقایسه می‌کند و ارقامش با `LuxUI.Fa` فارسی می‌شود). از این به بعد هر bump
  نسخه در csproj به‌صورت خودکار در نوار عنوان هم منعکس می‌شود؛ بازنویسی دستی لازم نیست و این
  کلاس باگ برای همیشه بسته شد
- **دامنه‌ی تغییر:** فقط یک بلوک ۳ خطی در `BuildTitleBar` + شماره نسخه در csproj + بازنویسی
  راهنمای پرتابل — هیچ تغییری در موتور اسکن، آپدیتر یا دیتابیس

---

## 🖥️✨🔧 نسخه ۳.۵.۰ — تثبیت زیرساخت (حاصل بازبینی کامل ۸هزار خطی سورس)

پس از بازبینی کامل پروژه در ۹ لایه، چهار اصلاح P0 + دو قابلیت P1 (بدون هیچ تغییری در موتور اسکن) اعمال شد.

### 🐛 باگ ۳۷ — شمارش اشتباه اعضای گروه مقایسه بعد از حذف محصول
- **ریشه:** `DeleteProduct` فقط ردیف `SavedProducts` را حذف می‌کرد؛ ردیف‌های `CompareItems`
  (لینک‌های عضویت محصول در گروه‌های مقایسه) **یتیم** می‌ماندند. `GetCompareGroups` تعداد اعضا را با
  `COUNT(i.Id)` روی خودِ `CompareItems` می‌شمارد (نه JOIN با محصولات) → محصولِ حذف‌شده در شمارنده‌ی
  گروه می‌ماند ولی در `GetGroupProducts` (که JOIN با SavedProducts دارد) نمایش داده نمی‌شد →
  «تعداد ۳» با دو کارت واقعی. همین مشکل برای «حذف همه‌ی محصولات یک دسته» هم رخ می‌داد
- **رفع (DatabaseManager.cs):**
  ۱) `DeleteProduct` → تراکنش دو مرحله‌ای: اول `DELETE FROM CompareItems WHERE ProductId=@Id`، بعد محصول
  ۲) `DeleteProductsInCategory` → تراکنش: اول پاک‌سازی لینک‌های محصولات آن دسته، بعد محصولات
  ۳) پاک‌سازی خودکار یتیم‌های موجود در هر استارت‌آپ:
     `DELETE FROM CompareItems WHERE ProductId NOT IN (SELECT Id FROM SavedProducts)` —
     idempotent است و داده‌های خراب قدیمیِ کاربران را هم بدون هیچ مهاجرتی ترمیم می‌کند

### 🐛 باگ ۳۸ — اجرای همزمان دو نسخه → SQLITE_BUSY و داده‌ی گم‌شده
- **ریشه:** هیچ مکانیزم تک‌نمونه‌ای وجود نداشت. فایل db اشتراکی است؛ دو پروسه با دو Connection
  نویسنده (حتی با WAL) رقابت می‌کنند: busy-timeout خطای SQLITE_BUSY می‌دهد یا نوشته‌ها صف و گم می‌شوند.
  کاربر به‌راحتی می‌تواند دوبار کلیک کند (دابل‌کلیک روی exe = دو نمونه!)
- **رفع (WindowsProcessGuard.cs جدید + App.xaml.cs):** Mutex نام‌دار `Local\BazarSanj.SingleInstance`
  در اولین خط OnStartup — نمونه‌ی دوم با پیام فارسی گویا بسته می‌شود. پیشوند `Local\` یعنی هر جلسه‌ی
  ویندوز مستقل است (چند کاربر همزمان RDP مشکلی ندارند). شکست خود مکانیزم هم (آنتی‌ویروس و غیره)
  برنامه را نمی‌بندد — رفتار عین قبل

### 🐛 باگ ۳۹ — ریشه‌کنی نهایی پروسسه‌های زامبی node/chrome (علت مادری باگ ۳۶)
- **ریشه:** درایور Playwright (node.exe) و کرومیوم، فرزندان پروسه‌ی برنامه‌اند. اگر برنامه وسط اسکن
  کرش کند یا از Task Manager بسته شود، این فرزندان **زامبی** می‌مانند (به والدِ مرده وصل‌اند ولی
  هیچ‌کس جمع‌شان نمی‌کند) و فایل‌های پوشه‌ی برنامه را قفل می‌کنند. لایه‌ی دفاعی اسکریپت آپدیتر (v3.2.2)
  آن‌ها را موقع نصب می‌کشد — ولی خودِ زامبی‌شدن همچنان در منشأ باقی بود
- **رفع (WindowsProcessGuard.cs جدید + App.xaml.cs):** هنگام استارت، کل پروسه در یک
  **Job Object** ویندوزی با فلگ `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` قرار می‌گیرد
  (`AssignProcessToJobObject` روی GetCurrentProcess). همه‌ی فرزندان آینده به‌طور خودکار عضو همان
  Job می‌شوند؛ به‌محض خروج پروسه‌ی برنامه (هر نوع — خروج تمیز، کرش، End Task، قطع برق ویندوز)
  هسته‌ی ویندوز کل اعضای Job را می‌کشد. **زامبی دیگر اصلاً متولد نمی‌شود.** هندل Job عمداً هرگز بسته
  نمی‌شود (بستن آن = کشتن برنامه) و کل مکانیزم در try/catch است — اگر در محیطی کار نکرد، رفتار عین قبل

### 🐛 باگ ۴۰ — لاگ بی‌صدا در پوشه‌های فقط-خواندنی
- **ریشه:** `Logger` همیشه در `AppContext.BaseDirectory` می‌نوشت؛ اگر پوشه‌ی برنامه فقط-خواندنی بود
  (Program Files، Desktop همگام‌شونده با OneDrive، ...)، `catch{}` خطا را قورت می‌داد و لاگ
  **کلاً وجود نداشت** — کاربر خطا می‌بیند ولی فایل لاگ خالی است و دیباگ غیرممکن می‌شود
- **رفع (Logger.cs + App.xaml.cs):** تشخیص یک‌باره‌ی اولین مسیر قابل‌نوشتن با پروب واقعی:
  کنار exe → `%APPDATA%\TorobScanner` (همان پناهگاه دیتابیس) → `%TEMP%`. چرخش ۵۱۲KB و thread-safety
  حفظ شد. `App.LogPath` هم حالا از `Logger.LogFilePath` می‌خواند تا پیام‌های خطا مسیر **واقعی** لاگ
  را نشان دهند، نه مسیر همیشه-فرضی کنار exe

### ✨ قابلیت ۱۲ — خلاصه‌ی آماری اسکن و بروزرسانی (فقط لایه UI)
- پایان اسکن خارجی: «اسکن {نام سایت} کامل شد — N جدید از M محصول در T ثانیه» در نوار وضعیت + اعلان
- پایان بروزرسانی همه: «بروزرسانی کامل شد — N محصول در T ثانیه»
- (`Stopwatch` در `ScanExternalSite_Click` و `RefreshAll_Click` — هیچ تغییری در مسیر اسکن)

### ✨ قابلیت ۱۳ — خروجی CSV کنار اکسل
- `ImportExportService.ExportToCsv`: UTF-8 **با BOM** (بدون BOM، اکسل فارسی را mojibake نشان می‌دهد)،
  نقل‌قول استاندارد RFC 4180 برای فیلدهای حاوی ویرگول/گیومه (اسم‌های فارسی پرتکرار ویرگول دارند)،
  اعداد با نقطه‌ی انگلیسی `InvariantCulture`
- پنجره‌ی ذخیره حالا دو فرمت دارد: اکسل / CSV — انتخاب خودکار متد بر اساس پسوند

### ⚡ بهینه‌سازی — `PRAGMA synchronous=NORMAL` (DatabaseManager.cs)
- کنار WAL موجود: ترکیب رسمی توصیه‌شده SQLite — fsync کامل فقط در چک‌پوینت WAL؛ سرعت نوشتن بالاتر
  بدون به‌خطر افتادن داده (در قطع برق، WAL تا آخرین تراکنش کاملاً سالم می‌ماند)

- نسخه ۳.۵.۰ — بیلد ۰ خطا / ۰ هشدار؛ ۸۱۳ فایل غیربرنامه‌ای بسته‌ی پرتابل بایت‌به‌بایت با ۳.۲.۲ یکسان
- ⚠️ آگاهانه انجام‌نشده: تغییر UA مرورگر (Chrome/120 روی موتور ۱۴۸)، waitForSelector، jitter و
  پروفایل‌های JSON سایت‌ها — طبق درخواست صریح کاربر، موتور اسکنی که الان «نتیجه می‌دهد» دست‌نخورده ماند

---
## 🖥️🔧 نسخه ۳.۲.۲ — بازنویسی موتور بروزرسانی خودکار

دستور کاربر: «چرا بعد از آپدیت نه برنامه آپدیت میشه نه اتوماتیک بعد آپدیت باز نمیشه» —
ریشه‌یابی سه‌لایه و رفع کامل.

### 🐛 باگ ۳۶ — آپدیت خودکار: نه نصب می‌شود، نه برنامه دوباره باز می‌شود
- **ریشه ۱ — قفل فایل توسط پروسسه‌های زامبی:** بعد از بسته‌شدن برنامه، node.exe و
  chrome-headless-shell باقی‌مانده از اسکن‌های قبلی زنده می‌ماندند (فرزندان Playwright به‌صورت
  خودکار با مرگ والد نمی‌میرند) و فایل‌های پوشه‌ی برنامه قفل بود → robocopy شکست می‌خورد
  (errorlevel 8) → اسکریپت مخفی روی `pause` برای همیشه می‌ماند → **نه فایل‌ها جایگزین می‌شد
  نه برنامه دوباره باز می‌شد** — دقیقاً همان علائم گزارش‌شده
- **ریشه ۲ — انتخاب کور asset:** آپدیتر همیشه «اولین zip» از Release را برمی‌داشت که بسته‌ی
  سبک Lite (بدون runtime) است؛ برای نصب پرتابل باید بسته‌ی هم‌نوع دانلود می‌شد وگرنه
  ترکیب ناقص/ناسازگار ممکن بود
- **ریشه ۳ — تداخل شماره نسخه:** بسته‌ی تستی میانی (با همان شماره ۳.۲.۱) باعث می‌شد آپدیتر
  نسخه‌ی جدید را «جدید» تشخیص ندهد

- **رفع (UpdateService.cs + UpdateWindow.cs):**
  ۱) اسکریپت نصب قبل از کپی، پروسسه‌های اجراشده از پوشه‌ی برنامه را هدفمند می‌بندد
     (`Get-Process | ForEach { if ($_.Path -like 'appDir*') { Stop-Process -Force } }`)
  ۲) شکست robocopy دیگر pause مخفی ندارد — خطا در `update_error.log` کنار برنامه ثبت و
     برنامه دوباره باز می‌شود (کاربر هرگز «گم» نمی‌شود)
  ۳) تشخیص نوع نصب با `hostfxr.dll` → انتخاب خودکار Portable/Lite هم‌نوع
  ۴) اسکریپت UTF-8 **بدون BOM** (BOM خط اول `@echo off` را خراب می‌کرد) + خواب با `ping`
     به‌جای `timeout` (در پنجره‌ی مخفی بی‌ورودی، timeout ممکن است شکست بخورد) + رتب ۵×۲ ثانیه‌ای robocopy
  ۵) دانلود: timeout از ۱۰ به ۳۰ دقیقه + یک تلاش مجدد خودکار + در صورت شکست نهایی، دکمه‌ی
     «باز کردن صفحه‌ی دانلود دستی» در همان پنجره

- نسخه ۳.۲.۲ — بیلد ۰ خطا / ۰ هشدار

---
## 🖥️✨ نسخه ۳.۲.۱ — رفع دو باگ رابط کاربری + منوی جمع‌شونده

سه دستور کاربر: ① «وقتی میخوام چیزی رو انتخاب کنم کل باکس سفید میشه» ② «پراگس بار از کل قسمت
کلی میزنه بیرون» ③ «برای تب کناریش یه گزینه بزاری که جمع بشه و هر موقع خواستم باز بشه تا توی
مانیتورهای کوچیک جلوه قشنگ‌تری داشته باشه» — هر سه انجام شد.

### 🐛 باگ ۳۴ — سفیدشدن باکس ComboBox هنگام انتخاب
- **ریشه:** در قالب ComboBox تم، `ToggleButton` هیچ `ControlTemplate` اختصاصی نداشت؛ WPF سراغ
  قالب پیش‌فرض Aero (ButtonChrome) می‌رفت که هنگام hover/focus پس‌زمینه‌ی گرادیان سفید-نقره‌ای
  می‌کشد و شیشه‌ی تیره‌ی تم را نابود می‌کرد (بررسی کل پروژه: هیچ استایل implicit دیگری برای
  ToggleButton وجود نداشت)
- **رفع (LuxTheme.xaml):** قالب شفاف اختصاصی برای ToggleButton
  (`<Border Background="Transparent"/>`) + تریگر جدید `IsDropDownOpen=True` → شیشه‌ی پررنگ‌تر
  (LuxGlassFillStrong) و فلش با رنگ اکسنت تم؛ حاشیه‌ی فوکوس نارنجی/آبی سیستم هم برای همیشه حذف شد

### 🐛 باگ ۳۵ — بیرون‌زدگی نوار پیشرفت (ProgressBar) از کادر
- **ریشه (تأییدشده از سورس رسمی dotnet/wpf — ProgressBar.cs):** در حالت `IsIndeterminate`، متد
  `SetProgressBarIndicatorLength` عرض `PART_Indicator` را برابر عرض کل ترک قفل می‌کند و چون
  نتیجه به‌صورت local-value ست می‌شود، از Setter قالب (MaxWidth) می‌چربد و انیمیشن از کادر
  بیرون می‌زند
- **رفع (LuxTheme.xaml):** بازنویسی کامل قالب ProgressBar — برش اجباری (`ClipToBounds`) داخل
  PART_Track + لایه‌ی glow مستقل (عرض ۶۴px، حرکت ‎−۸۰ تا ۹۰۰ در ۱.۴s) که فقط داخل محدوده دیده
  می‌شود + `PART_Indicator` با Opacity صفر در حالت نامعین تا WPF آزادانه عرضش را مدیریت کند +
  تریگر RTL برای جهت درست حرکت در رابط فارسی

### ✨ فیچر ۱۱ — منوی کناری جمع‌شونده (مانیتورهای کوچک)
- **دستگیره‌ی شیشه‌ای با فلش** (Path برداری — مستقل از فونت) بالای لبه‌ی داخلی سایدبار؛ در هر
  دو حالت جای ثابت دارد و با انیمیشن ۲۸۰ms می‌چرخد
- **انیمیشن نرم ۳۰۰ms** (CubicEase): عرض سایدبار ۲۵۲ ↔ ۶۶px + محو متقابل دو لایه (نمای کامل ↔
  ریل آیکونی)؛ ستون Auto باعث می‌شود فضای کاری هم‌زمان و نرم جا باز کند
- **ریل آیکونی:** هر ۶ دکمه با همان عملکرد و همان هندلرهای حالت باز + ToolTip فارسی + نبض سبز
  وضعیت موتور؛ ~۱۸۶px فضا به جدول محصولات آزاد می‌شود
- **ماندگاری وضعیت:** `SidebarCollapsed` به AppSettings اضافه شد و در settings.json ذخیره
  می‌شود؛ هنگام شروع برنامه، بدون انیمیشن اعمال می‌گردد
- **ضدباگ RTL:** لایه‌ی میزبان دستگیره صریحاً LTR است تا جای فلش از آینه‌سازی راست‌به‌چپ مستقل
  باشد؛ ناحیه‌ی ۵۶px بالای ریل برای دستگیره رزرو شده تا با دکمه‌ها تداخل نکند؛ محافظ
  `_sidebarAnimating` از دابل‌کلیک وسط انیمیشن جلوگیری می‌کند

- نسخه ۳.۲.۱ — بیلد ۰ خطا / ۰ هشدار

---
## 🖥️📦 نسخه ۳.۲.۰ — رفع کامل «ارور node.exe هنگام اسکن» + بسته پرتابل + بهبود اسکن کافه‌کالا

دو دستور کاربر: ① «مشکل کافی‌کالا هنوز حل نشده» ② «یه فایل exe پرتابل درست کن که node.exe هم داخل
خود پروژه باشه، چون موقع اسکن ارور node.exe می‌ده» — هر دو انجام شد.

### 🐛 باگ ۳۲ — «ارور node.exe» هنگام اسکن سایت خارجی
- **ریشه (تحلیل کد منبع Playwright 1.60):** مایکروسافت Playwright هیچ‌چیز در زمان اجرا extract
  نمی‌کند؛ صرفاً دنبال «`.playwright\node\win32_x64\node.exe`» **کنار Microsoft.Playwright.dll**
  می‌گردد و اگر آنجا نباشد (ZIP بلاک‌شده‌ی ویندوز / قرنطینه‌ی آنتی‌ویروس / کپی ناقص پوشه)،
  خطای «`Driver not found: ...node.exe`» می‌دهد و اسکن خارجی هیچ‌وقت شروع نمی‌شود
- **رفع — حالت پرتابل در BrowserLauncher (استاتیک‌ساز):** اگر پوشه‌های باندل کنار exe باشند،
  مسیرها با متغیرهای محیطی رسمی Playwright قفل می‌شوند:
  `PLAYWRIGHT_NODEJS_PATH` (node.exe باندل‌شده)، `PLAYWRIGHT_DRIVER_SEARCH_PATH` (درایور باندل‌شده)،
  `PLAYWRIGHT_BROWSERS_PATH` (کرومیوم باندل‌شده) → برنامه کاملاً مستقل از سیستم اجرا می‌شود
- **پیام فارسی گویا:** اگر درایور/node.exe گم باشد، به‌جای خطای انگلیسی مبهم، پیام سه‌مرحله‌ای
  فارسی (جابه‌جایی پوشه‌ها / استثنای آنتی‌ویروس / Unblock کردن ZIP) نمایش داده می‌شود
- **بسته پرتابل رسمی:** `BazarSanj-3.2.0-Portable-win-x64.zip` — بدون نصب .NET (self-contained)،
  بدون نصب Node.js (node.exe داخل بسته: `node\node.exe` + داخل درایور)، بدون نصب مرورگر
  (کرومیوم headless-shell ۱۲۲۳ داخل `browsers\` با نشانگر INSTALLATION_COMPLETE) +
  لایه‌های پشتیبان Edge/Chrome مطابق قبل

### 🐛 باگ ۳۳ — اسکن کافه‌کالا: انتظار هوشمند به‌جای صرف NetworkIdle کورکورانه
- **ریشه کندی/شکنندگی:** بعد از هر کلیک «بارگیری بیشتر»، ۱۰s برای NetworkIdle + ۱.۲s مکث ثابت
  منتظر می‌ماندیم → ~۱۲ ثانیه به‌ازای هر کلیک (۱۵ کلیک ≈ ۳ دقیقه) و اگر AJAX بی‌صدا شکست
  می‌خورد، بی‌خبر ادامه می‌یافت
- **رفع:** شمارش کارت‌های محصول پس از هر کلیک پول می‌شود (تا ۱۵s — پنجره رشد)؛ دو راند
  پشت‌سرهم بدون محصول جدید = پایان لیست → قطع تمیز. سلکتور اختصاصی پروفایل
  (`LoadMoreSelectors`: `a.wd-load-more.load-on-click`) قبل از لیست عمومی امتحان می‌شود
- **تست زنده‌ی e2e:** ۱۵ کلیک مؤثر در **۳۴ ثانیه** (قبلاً ~۳ دقیقه) → **۱۲۳ محصول یونیک**؛
  سپس rel=next به /page/2/ → ریدایرکت به خانه → محافظ ریدایرکت (باگ ۲۸) قطع کرد ✓
- رگرسیون ننجون (زنجیره rel=next) و فیلتر فروش‌رفته: هر دو سالم ✓

- نسخه ۳.۲.۰ — بیلد ۰ خطا / ۰ هشدار

---

## 🛒✨ نسخه ۳.۱.۴ — رفع اسکن ناقص کافه‌کالا + جستجوی زنده‌ی محصولات

دو دستور کاربر: «این سایت هم فقط ۱۵ تا محصول شناسایی می‌کنه» (کافه‌کالا — دسته‌ی کپسول قهوه) و
«بعدش یه جایی واسه سرچ محصولات هم بزار» — هر دو انجام شد؛ تست‌ها روی سایت زنده و HTML واقعی.

### 🐛 باگ ۲۸ — کافه‌کالا فقط ~۱۵ محصول اسکن می‌شد (باید ~۱۲۰ باشد)
- **ریشه (تحلیل زنده):** افزونه‌ی سئو **Rank Math** آدرس `/page/2/` را با **کد ۳۰۱ به صفحه‌ی اصلی
  ریدایرکت می‌کند**؛ پارامترهای `?paged` / `?page` / `?offset` / `?product-page` هم کورکورانه
  همان صفحه‌ی اول را برمی‌گردانند → اسکنر بعد از صفحه‌ی اول به «صفحه‌ی اصلی سایت» می‌رفت و
  محصولات سر راهیِ ویجت‌های خانه را هم می‌چید (~۷ مورد = همان ۱۵ گزارشی!)
- **ساختار واقعی صفحه‌بندی:** دکمه‌ی AJAX **«بارگیری بیشتر محصولات»**
  (`a.wd-load-more.wd-products-load-more.load-on-click` — هر کلیک ۸ محصول)

### 🚫 رفع (دو بخش)
1. **پروفایل کافه‌کالا:** حالت `NeedsLoadMore` (کلیک دکمه‌ی بارگیری بیشتر) + سقف ۳۰ کلیک
   — موتور از قبل سلکتور `.wd-load-more` را پشتیبانی می‌کرد؛ فقط فعال‌سازی لازم بود
2. **محافظ ریدایرکت در موتور اسکن (جهانی):** اگر صفحه‌ی بعدیِ زنجیره به آدرس دیگری
   (مثلاً صفحه اصلی) پرت شد، محتوای مقصد استخراج **نمی‌شود** و زنجیره تمیز قطع می‌شود —
   دیگر هیچ سایتی نمی‌تواند محصولات بی‌ربط به لیست تزریق کند
   (مقایسه فقط host+path؛ پارامتر/اسلش/انکودینگ فارسی ریدایرکت حساب نمی‌شود)

### 🔍 قابلیت جدید — جستجوی زنده‌ی محصولات
- کادر جستجو بالای لیست محصولات: جستجوی همزمان در **نام محصول، فروشگاه و دسته**
- نرمال‌سازی فارسی: «ي/ی»، «ك/ک» و نیم‌فاصله در جستجو تفاوتی نمی‌کنند
- زنده با تاخیر ۳۲۰ms (بدون کندی هنگام تایپ) + پاک‌سازی سریع با **ESC**
- خالی‌استیت اختصاصی «نتیجه‌ای برای «…» پیدا نشد»

### ✅ تست‌های انجام‌شده (سایت زنده)
- کافه‌کالا end-to-end: ۱۵ کلیک «بارگیری بیشتر» → **۱۲۳ محصول یونیک با قیمت**
  (قبلاً ۱۵)؛ سپس rel=next به /page/2/ → ریدایرکت به خانه → محافظ قطع کرد ✓
- رگرسیون ننجون: ۱۲/۱۲ محصول موجود + فیلتر فروش‌رفته هنوز سالم ✓
- بیلد Release: ۰ خطا / ۰ هشدار

- نسخه ۳.۱.۴ — بیلد ۰ خطا / ۰ هشدار

---

## 🚫🛒 نسخه ۳.۱.۳ — رفع ورود محصولات «فروش‌رفته/ناموجود» به اسکن

بازخورد کاربر: «الان محصولاتی که فروش رفته هم داره میاره — این لینک رو که اسکن کرده رو باز می‌کنی
زده فروش رفته؛ اینو درست کن که فقط محصولات موجود رو بیاره» (محصول ننجون: کپسول دولچه گوستو لونگو اینتنسو)
— ریشه‌یابی با تحلیل زنده‌ی سایت + رفع سه‌لایه‌ای + تست عملی روی HTML واقعی.

### 🐛 باگ ۲۷ — محصولات فروش‌رفته با قیمت معتبر وارد اسکن می‌شدند
- **ریشه:** دسته‌بندی ننجون یک JSON-LD کامل دارد (ItemList با قیمت هر محصول)؛ استراتژی JSON-LD برنده می‌شود.
  محصولات فروش‌رفته هم در همین JSON-LD **قیمت معتبر** دارند و تنها نشانه‌ی وضعیت‌شان فیلد
  `"availability": "http://schema.org/OutOfStock"` است که استخراج‌گر قبلاً **کلاً نادیده می‌گرفت**
- **الگوی واقعی روی سایت (تأیید زنده):** محصول فروش‌رفته = قیمت `8500000` + `availability: OutOfStock`
  + کلاس ووکامرسی `outofstock` روی کارت + برچسب تم وودمارت «فروخته شد»

### 🚫 رفع سه‌لایه‌ای (در هر ۳ استراتژی استخراج — جهانی برای همه‌ی سایت‌ها)
1. **JSON-LD:** offerهای `OutOfStock`/`SoldOut` کنار گذاشته می‌شوند؛ اگر **تمام** offerهای دارای
   availability ناموجود باشند → کل محصول skip (محصول ترکیبی فقط از offer موجود قیمت می‌گیرد)
2. **Microdata:** `itemprop="availability"` با مقدار OutOfStock → رد شدن
3. **DOM:** کارت‌هایی که کلاس استاندارد `outofstock`/`out-of-stock` دارند یا برچسب
   «فروخته شد»، «فروش رفته»، «ناموجود»، «موجود نیست»، «اتمام موجودی»، «sold out» دارند → رد شدن
   (چک متن فقط روی برگ‌های کوتاه ≤۳۲ کاراکتر تا اسم محصول false positive ندهد)

### ✅ تست‌های انجام‌شده (Playwright روی HTML واقعی ننجون)
- صفحه دسته‌بندی (۱۲ محصول موجود): **۱۲/۱۲ استخراج شد** — صفر false positive
- صفحه‌ی محصول فروش‌رفته‌ی گزارشی + کاروسل مرتبط: محصول اصلی (JSON-LD OutOfStock) و
  هر ۳ کارت فروش‌رفته‌ی مرتبط (کلاس outofstock + برچسب «فروخته شد») همگی فیلتر شدند —
  فقط محصولات موجود در خروجی
- بیلد Release: ۰ خطا / ۰ هشدار

- نسخه ۳.۱.۳ — بیلد ۰ خطا / ۰ هشدار

---

## 🕷️✨ نسخه ۳.۱.۲ — رفع اسکن ناقص سایت ننجون (فقط صفحه ۱ اسکن می‌شد)

بازخورد کاربر: «برای سایت ننجون این فقط پیج اول رو اسکن می‌کنه و بقیه محصولات رو نمی‌خونه» —
ریشه‌یابی با اسکن زنده‌ی سایت + رفع + تست عملی روی خود سایت.

### 🐛 باگ ۲۶ — سایت‌های «لود-آن-اسکرول» فقط یک صفحه اسکن می‌شدند
- **ریشه:** تم Woodmart در ننجون (nanjuncoffee.com) صفحه‌بندی را **AJAX** انجام می‌دهد؛
  در بدنه‌ی صفحه هیچ لینک قابل‌کلیکی (`a.next` / `.page-numbers`) وجود ندارد که موتور دنبالش بگردد —
  صفحه‌بندی واقعی فقط با **تگ سئوی `<link rel="next">` در `<head>`** اعلام می‌شود
- **نتیجه‌ی قبلی:** موتور «صفحه‌ی بعد» را پیدا نمی‌کرد → اسکن بعد از ۱۲ محصول صفحه‌ی اول قطع می‌شد
- **راه‌حل:** موتور `findNext` حالا بعد از لینک‌های معمولی، **`<link rel="next">` در head را هم می‌خواند**
  (این تگ استاندارد سئو است و ده‌ها سایت ووکامرسی آن را دارند → رفع به نفع همه‌ی سایت‌هاست)

### 🕷️ بهینه‌سازی پروفایل ننجون
- تست زنده: دسته‌ی «کپسول قهوه» **~۱۹ صفحه × ۱۲ محصول ≈ ۲۲۰ محصول** دارد (صفحه ۲۰ → 404)
- سقف صفحات پروفایل ننجون: ۱۰ → **۲۵ صفحه**
- `ForceTryPagination` هم فعال شد (نگهبان: اگر روزی rel=next حذف شد، موتور /page/N/ را امتحان می‌کند و
  محافظ «محصول تکراری = توقف» جلوی حلقه را می‌گیرد)
- پایان صفحه‌بندی تمیز تشخیص داده می‌شود: صفحه‌ی 404 بدون محصول → توقف خودکار

### ✅ تست‌های انجام‌شده
- زنجیره‌ی صفحه‌بندی در کرومیوم واقعی: صفحه ۱ → ۲ → ۳ → ۴ همه با `<link rel="next">` پیدا شدند
- هر صفحه ۱۲ محصول (۳۸/۳۶/۳۶/۳۶ لینک خام که موتور به ۱۲ محصول یونیک تبدیل می‌کند)
- بیلد از داخل ZIP: ۰ خطا / ۰ هشدار

- نسخه ۳.۱.۲ — بیلد ۰ خطا / ۰ هشدار

---

## ⌨️✨ نسخه ۳.۱.۱ — رفع مشکل فضای تایپ در کادرهای متنی

بازخورد کاربر: «وقتی می‌خوام تو باکس‌ها متن یا چیزی بنویسم نمی‌تونم، انگار فضاش کمه —
متن گم می‌شه و نشون داده نمی‌شه حین تایپ» — ریشه‌یابی و رفع شد.

### 🐛 باگ ۲۵ — متن هنگام تایپ در کادرها ناپدید می‌شد (علت اصلی)
- **ریشه:** فونت وزیرمتن خطِ عمودی بلندی دارد (~۲۳px در اندازه‌ی ۱۳) اما کادرهای متنی
  **ارتفاع ثابت ۳۶–۴۰px** به‌همراه پدینگ داخلی عمودی ۱۴px داشتند → فضای مؤثرِ خط از خودِ خط کوچک‌تر
  می‌شد و هنگام تایپ، متن و نشانگر (کرسر) به بیرون از ناحیه‌ی دید اسکرول می‌شدند — انگار چیزی تایپ نمی‌شود
- **راه‌حل در تم:** پدینگ عمودی کادر کاهش یافت (۷→۴) + **حداقل‌ارتفاع ۴۰px برای همه‌ی کادرهای متنی** +
  متن همیشه عمودی‌وسط‌چین

### ⌨️ اصلاح سراسری هر ۸ کادر متنی برنامه
- کادرهای «افزودن محصول»، «اسکن خارجی»، «دسته‌بندی‌ها»، «نام گروه جدید» و «جستجوی تب مقایسه»
  از ارتفاع ثابت به **MinHeight** تبدیل شدند → کادر هرگز کوچک‌تر از یک خط کامل نمی‌شود و
  در صورت نیاز به‌طور خودکار بزرگ می‌شود
- دکمه‌های کنار کادرها هم‌قدِ ۴۰px شدند تا ردیف‌ها صاف بمانند (افزودن دسته، دکمه‌ی ＋ و ساخت گروه)
- باکس یادداشت‌های انتشار (فقط‌خواندنی و چندخطی با اسکرول) عمداً بدون تغییر ماند

- نسخه ۳.۱.۱ — بیلد ۰ خطا / ۰ هشدار

---

## ⇄✨ نسخه ۳.۱.۰ — تب مقایسه محصولات: لینک محصولات فروشگاه‌های مختلف

درخواست کاربر: «می‌خوام یه تب جدید باز شه وقتی کلیک می‌کنی رو محصولات تا بتونیم محصولات فروشگاه‌های
مختلف رو با هم لینک کنیم — مثلا ارپیچیو سام‌کیش رو با ارپیچیو کافی‌کالا — و قیمت‌هاشون رو داخل همون تب» — پیاده شد.

### ⇄ نوار تب‌ها + باز شدن تب با کلیک روی محصول
- **نوار تب بالای فضای کاری**: تب ثابت «همه محصولات» + تب‌های مقایسه‌ی قابل بستن (✕)
- **کلیک روی هر کارت محصول → تب مقایسه‌ی همان محصول باز می‌شود** (کارت‌ها حالا دست‌خط Cursor و
  تولتیپ راهنما دارند؛ دکمه‌ی ↗ لینک همان‌طور مستقل کار می‌کند)
- دکمه‌ی جدید سایدبار **«⇄ مقایسه محصولات»** هم اضافه شد — هاب مقایسه برای ساخت گروه از صفر
- بعد از تعویض تم، تب‌های باز مقایسه دوباره باز می‌شوند و تب فعال حفظ می‌شود

### 🔗 گروه‌های مقایسه (لینک محصولات)
- داخل هر تب مقایسه، یک **«گروه مقایسه»** ساخته می‌شود (مثلا «ارپیچیو») — محصول کلیک‌شده **خودکار عضو** می‌شود
- با **جستجوی زنده** (که از قبل با نام محصول پر شده) همان کالا از فروشگاه دیگر پیدا و با «+ افزودن» لینک می‌شود
- عضویت تکراری خودکار رد می‌شود؛ حذف عضو با دکمه‌ی ✕ روی کارت؛ حذف کل گروه با تأیید —
  **محصولات از لیست اصلی پاک نمی‌شوند** (فقط لینک‌ها)
- گروه‌ها در SQLite ذخیره می‌شوند (جدول‌های `CompareGroups` / `CompareItems`) — بعد از بستن برنامه هم می‌مانند

### 💱 نمایش قیمت‌ها کنار هم
- اعضای گروه مرتب‌شده از ارزان‌ترین به گران‌ترین با کارت‌های هم‌سبک لیست اصلی
- **نشان «✦ ارزان‌ترین»** (سبز) برای ارزان‌ترین فروشگاه + **«▲ x٪ گران‌تر»** برای بقیه + «در انتظار قیمت» برای بدون‌قیمت‌ها
- چیپ‌های آمار گروه: تعداد فروشگاه‌ها / ارزان‌ترین قیمت / اختلاف قیمت (درصدی)
- دکمه‌ی **⟳ بروزرسانی قیمت‌های این گروه** — فقط اعضای گروه اسکن می‌شوند (قابل توقف با ⏹)
- سربرگ تب، خلاصه‌ی محصول کلیک‌شده (فروشگاه، دسته، آخرین بروزرسانی، قیمت، لینک) را نشان می‌دهد

### جزئیات فنی
- صفحه‌ی مقایسه با هر بار فعال‌سازی تب از نو ساخته می‌شود → بعد از اسکن/تغییرات همیشه قیمت‌های تازه
- بعد از «بروزرسانی لیست»، اسکن خارجی و مدیریت دسته‌بندی‌ها، تب مقایسه‌ی فعال خودکار بازسازی می‌شود
- نوار وضعیت + دکمه توقف حالا بین همه‌ی تب‌ها مشترک است (پیشرفت عملیات از هر تبی دیده می‌شود)
- نسخه ۳.۱.۰ — بیلد ۰ خطا / ۰ هشدار | تست publish (win-x64) موفق | کوئری‌های SQL تست شدند

---

## 🐛✨ نسخه ۳.۰.۱ — رفع ارور تعویض تم + لطیف‌سازی هاله‌های پس‌زمینه

بازخورد کاربر: «تم رو همون اول عوض نمی‌کنه، باید باز و بسته کنی و ارور می‌ده حین تعویض تم» +
«تم کامل بلور نشده و دایره‌ها خیلی بزرگ و زننده تو چشم‌اند» — هر دو ریشه‌یابی و حل شد.

### 🐛 باگ ۲۱ — ارور «Cannot change AllowsTransparency» هنگام تعویض تم (علت اصلی)
- **ریشه:** تعویض تم، محتوای پنجره را بازسازی می‌کرد اما در همان مسیر، ویژگی‌های کروم پنجره
  (از جمله `AllowsTransparency`) دوباره ست می‌شد — WPF اجازه‌ی تغییر شفافیت پنجره‌ی **باز** را نمی‌دهد
- **نتیجه‌ی قبلی:** ارور + تم فقط بعد از بستن و بازکردن برنامه اعمال می‌شد
- **راه‌حل:** کروم پنجره (اندازه، شفافیت، WindowChrome و...) فقط بار اول تنظیم می‌شود؛
  تعویض تم فقط محتوا را بازسازی می‌کند → **تعویض زنده‌ی واقعی، بدون ارور و بدون ری‌استارت**
- هدیه‌ی جانبی: اگر اندازه‌ی پنجره را تغییر داده باشید، تعویض تم دیگر آن را به هم نمی‌زند

### 🐛 باگ ۲۲ — سحابی‌ها به‌جای هاله‌ی بلور، دایره‌ی مات با لبه‌ی تیز بودند
- **ریشه:** تابع ساخت «لبه‌ی شفاف» گرادیان سحابی اشتباهاً رنگ کاملاً مات (آلفا ۲۵۵) برمی‌گرداند —
  گرادیان برعکس می‌شد: مرکز شفاف و **لبه‌ی کاملاً مات و تیز**
- **نتیجه:** حالا هاله‌ها واقعاً بلور و نرم‌اند — همان جلوه‌ی سحابی ویژن‌پرو

### 🐛 باگ ۲۳ — دایره‌ها خیلی بزرگ و زننده بودند
- سحابی‌های گلس‌مورفیسم ~۴۰٪ کوچک‌تر شدند (۷۶۰px → ۴۴۰px) و شدت تنفسشان کم‌جان‌تر
- هاله‌های آرورا و پرده‌ی طلای کوتور هم به همان نسبت کوچک‌تر و لطیف‌تر شدند
- امضای بصری هر تم حفظ شد — فقط دیگر چشم را نمی‌زند

### ✨ بهبود ۲۴ — پنجره‌ی تنظیمات هم زنده عوض می‌شود
- قبلاً با تعویض تم، خود پنجره‌ی تنظیمات ظاهر قدیمی نگه می‌داشت تا ببندید و باز کنید
- حالا همزمان با پنجره‌ی اصلی، بازسازی زنده می‌شود (با جلوگیری از نشت حافظه‌ی رخداد استاتیک)

### زیرساخت فنی
- نسخه ۳.۰.۱ — بیلد ۰ خطا / ۰ هشدار | تست publish (win-x64) موفق
- تم سفید «بنتو سفید» و بقیه‌ی تم‌های ۳.۰.۰ بدون تغییر

---


## 🤍✨ نسخه ۳.۰.۰ — تم دوم کاملاً سفید شد (ویژه‌ی عاشقان لایت‌مود)

بازخورد کاربر: «تم دومی رو کامل سفید طراحی کن که کاربری که لایت‌مد دوست داشته باشه بتونه استفاده کنه» — انجام شد.
تم «بنتو گرید» با بوم خاکستری مات (#F5F5F7) جای خود را به **«بنتو سفید» — Pure White Bento** با بوم **سفید خالص #FFFFFF** داد:

### بازطراحی بنتو سفید
- **بوم کاملاً سفید**: گرادیان پس‌زمینه، سایدبار و فضای کار همگی سفید خالص — هیچ ته‌رنگ خاکستری‌ای باقی نمانده
- **جداسازی با خط مویی اپل**: چون دیگر سایه و تفاوت رنگ بوم در کار نیست، کاشی‌ها با خط مویی کاملاً مات #D2D2D7 (رنگ استاندارد جداکننده‌ی اپل) ترسیم می‌شوند — دقیقاً مثل apple.com
- **هویت مسطح بنتو حفظ شد**: بدون سایه، شعاع بزرگ ۲۰px، تایپ درشت Bold — امضای طراحی برنده‌ی اپل
- **چیپ‌های خاکستری روشن #F5F5F7** برای عناصر تعاملی (ورودی‌ها، سوییچ‌ها، چیپ آیکون‌ها) — همان زبان اپل روی بوم سفید
- **ستاره‌ی لوگو سفید** روی چیپ متالی آبی اپل (قبلاً ستاره‌ی تیره روی آبی کنتراست ضعیفی داشت)
- آبی کلاسیک اپل #0071E3، دکمه‌ی اصلی آبی تخت با متن سفید، انتخاب سایدبار با تینت #E8F0FE

### پالایش لایت‌مود در کل برنامه
- **SelectionBrush** کادر متنی از مقدار ثابت به منبع داینامیک LuxSelection منتقل شد → رنگ انتخاب متن در هر تم از آکسنت همان تم پیروی می‌کند
- **نشان «✦ محصول جدید»** اصلاح شد: پس‌زمینه‌ی سبزِ اشباع به تینت ملایم ۲۰٪ تغییر کرد (هماهنگ با بقیه‌ی نشان‌ها؛ در تم سفید کنتراست را خراب نمی‌کند)
- پیش‌نمایش مینیاتیور تنظیمات برای «بنتو سفید» به‌روزرسانی شد

### بقیه‌ی تم‌ها دست‌نخورده ✓
پلاتینیوم گلس / آرورا گلو / کوتور نویر — بدون تغییر

### زیرساخت فنی
- تعویض زنده‌ی تم بدون ری‌استارت + ذخیره‌ی خودکار در settings.json (مثل قبل)
- پرفورمنس حفظ شد: بنتو سفید بدون سایه‌ی کارت = سبک‌ترین تم ممکن برای اسکرول روان
- نسخه ۳.۰.۰ — بیلد ۰ خطا / ۰ هشدار | تست publish (win-x64) موفق | رشته‌های تم جدید در DLL تأیید شد

---


## 🏆✨ نسخه ۲.۹.۰ — بازطراحی تم‌های ۲ تا ۴ بر اساس سبک‌های برنده‌ی جوایز طراحی

بازخورد کاربر: «۲ تا ۴ رو اصن خوشم نیومد — بر اساس ترندهایی که جایزه گرفتن طراحی کن» — حل شد.
تحقیق روی برندگان جوایز معتبر طراحی (Awwwards SOTD، FWA، اکوسیستم Apple Design) انجام شد و
سه زبان طراحیِ کاملاً متمایز جایگزین تم‌های قبلی شد:

### ۱) بنتو گرید — Bento Grid (جایگزین نئو بروتال)
- زبان طراحی **اپل** (ترند keynote و bento.me): بوم مات روشن #F5F5F7
- کاشی‌های سفید کاملاً مات و مسطح با **خط مویی خاکستری** — بدون هیچ سایه‌ای
- تایپوگرافی درشت **Bold** سیاهِ اپل + آبی کلاسیک #0071E3
- گوشه‌های بزرگ ۲۰px اما حس ساخت‌یافته‌ی «کارتن بنتو»
- دکمه اصلی: آبی تخت اپل | انتخاب سایدبار: تینت آبی روشن #E8F0FE

### ۲) آرورا گلو — Aurora Glow (جایگزین سایبر نئون)
- امضای **Linear/Stripe** (برندگان مکرر Awwwards SOTD): شب سرمه‌ای عمیق
- **سه هاله‌ی زنده‌ی تنفس‌دار** بنفش/فیروزه‌ای/صورتی در پس‌زمینه
- متن‌های آماری با **گرادیان آرورا** بنفش←فیروزه‌ای←صورتی
- کارت‌ها با هاله‌ی بنفش خیلی ملایم + بوردر بنفش ۲۰٪
- دکمه اصلی: گرادیان عمودی بنفش←آبی فیروزه‌ای

### ۳) کوتور نویر — Couture Noir (جایگزین کلود مینیمال)
- لوکس **خانه‌های مد** (سایت‌های Gucci/Dior برنده‌ی FWA): ابسیدین گرم + طلای شامپاینی
- **خطوط مویی طلایی** به‌جای سایه — ظرافت مینیمال مدروزانه
- گوشه‌های تیز و کم‌شعاع (۸px کارت) + تیترهای **Light** لطیف
- قیمت‌ها با **گرادیان طلای** شامپاین←طلای کلاسیک←برنز
- دکمه اصلی: متال طلایی با **متن مشکی** — امضای کوتور

### پلاتینیوم گلس (تم ۱) بدون تغییر باقی ماند ✓

### زیرساخت فنی
- `ThemeDecor` جدید: `Plain` (بنتو) / `Aurora` / `Couture` — دکور پس‌زمینه‌ی اختصاصی هر سبک در MainWindow
- پیش‌نمایش‌های مینیاتیور تنظیمات برای هر زبان بازنویسی شد (هاله‌های آرورا، قاب طلایی کوتور، کاشی مات بنتو)
- سازگاری: کاربرانی که قبلاً تم‌های حذف‌شده را انتخاب کرده بودند، خودکار به «پلاتینیوم گلس» برمی‌گردند
- تعویض زنده بدون ری‌استارت + ذخیره‌ی خودکار در settings.json (مثل قبل)
- پرفورمنس حفظ شد: همه‌ی انیمیشن‌های دکور ۲۰ فریم (Slow) + براش‌های Freeze
- نسخه ۲.۹.۰ — بیلد ۰ خطا / ۰ هشدار | تست publish (win-x64) موفق

---

## 🎨✨ نسخه ۲.۸.۰ — ۴ سبک طراحی کاملاً متفاوت (نه فقط رنگ!)

بازخورد کاربر: «تماها خیلی شبیه هم بودند» — حل شد. در ۲.۷ هر تم فقط پالت رنگی عوض می‌کرد و
شکل‌ها یکسان بودند. در ۲.۸ هر تم یک **زبان طراحی کامل** با هویت بصری مستقل است:

### ۱) پلاتینیوم گلس — گلس‌مورفیسم (ترند اپل ویژن‌پرو)
- شیشه‌های مات نیمه‌شفاف روی ابسیدین، گوشه‌های بزرگ ۱۴-۱۸px، خط مویی ۱px
- هاله‌های نرم + سحابی‌های زنده‌ی تنفس‌دار در پس‌زمینه
- دکمه اصلی: متالیک پلاتینیوم | سایه: فقط هنگام hover (گلو)

### ۲) نئو بروتال — بروتالیسم نو (ترند ۲۰۲۵ Gumroad/Figma)
- کاغذ کرم + شبکه نقطه‌ای | کارت‌های سفید با **خط دور سیاه ۲px**
- **سایه‌ی سخت آفست بدون بلور** (امضای بروتالیسم) | گوشه‌های تیز ۴px
- دکمه اصلی: زرد الکتریکی با متن مشکی | تیترها Bold

### ۳) سایبر نئون — HUD سایبری
- مشبک فنی تیره + گوی نور نئونی | کارت‌های تیره با لبه‌ی نئونی درخشان (گلو دائم)
- گوشه‌های تند ۴px | گرادیان‌های نئونی یخی برای قیمت‌ها | اسکرول‌بار نئونی

### ۴) کلود مینیمال — مینیمال ابری روشن (ترند Notion/Linear)
- تم **روشن**: کارت‌های سفید شناور **بدون خط دور** با سایه‌ی پخش نرم
- بزرگ‌ترین شعاع گوشه (۲۲px پنجره) | بنفش پاستلی | فضای آرام و خنک

### زیرساخت فنی
- `LuxStyle`: هر تم حالا شامل ~۴۰ توکن سبک است (شعاع ۸ نوع، ضخامت خط دور، جنس سایه، دکور پس‌زمینه، وزن تیتر، براش‌های hover/popup/scrollbar و...)
- `LuxTheme.xaml`: همه‌ی مقادیر هاردکد (شعاع ۱۱px شیشه‌ای، hover سفید، پاپ‌آپ تیره و...) با `DynamicResource` جایگزین شد → تعویض زنده‌ی «شکل» هم بدون ری‌استارت
- دکور پس‌زمینه به‌ازای سبک ساخته می‌شود: سحابی/نقطه‌ای/مشبک/هاله — همه استاتیک یا ۲۰فریم (بدون فریز)
- پیش‌نمایش هر تم در تنظیمات حالا **ظاهر واقعی همان سبک** را نشان می‌دهد (سایه سخت بروتال، گلو نئونی سایبر، کارت بی‌حاشیه کلود و...) + برچسب سبک (گلس‌مورفیسم/بروتالیسم/سایبرپانک/مینیمال)
- رفع ناسازگاری تم‌های روشن: hover/selected/popup/tooltip/scrollbar/دکمه‌های خطر و موفق همه برای پس‌زمینه‌ی روشن مقدار مستقل دارند
- سایه‌ی پایه‌ی کارت‌ها فقط برای سبک‌های سایه‌دار ساخته می‌شود (گلس مثل قبل گلو hover دارد) — پرفورمنس حفظ شد
- پنجره‌های مدیریت/اسکن/افزودن/بروزرسانی همگی با توکن‌های سبک هم‌گام شدند

---

---

# 📋 آرشیو تغییرات — نسخه ۲.۵.۰ تا ۲.۶.۰ «Platinum Glass»

**تاریخ:** ۱۴۰۴/۰۶/۰۹ — بر پایه نسخه ۲.۴.۱ | هات‌فیکس استارتاپ: ۱۴۰۴/۰۶/۱۰ و ۱۴۰۴/۰۶/۱۰

این گزارش شامل **همه تغییرات اعمال‌شده روی پروژه Price-Scanner 2.4.1** است:
۴ باگ رفع‌شده + ۱ محدودیت جدی UI حل‌شده + بازطراحی کامل ظاهری + هات‌فیکس ۲.۵.۱ برای کرش استارتاپ.

---

---

---

## ✨✨ نسخه ۲.۶.۰ — پالیش عمیق «روونی» + آیکون لوکس

### ۱) آیکون اختصاصی بازارسنج
- طراحی وکتوری: سکوئرکل ابیسی + ستاره چهارپر ✦ پلاتینیومی ایریدسنت (نماد برند) + خط صعودی قیمت + برق‌های شیشه‌ای
- خروجی چندسایزی (۱۶ تا ۲۵۶ پیکسل) → آیکون فایل exe، تسک‌بار، Alt+Tab و تیتر همه‌ی ۶ پنجره
- csproj: `ApplicationIcon` + Resource باندل‌شده در اسمبلی

### ۲) روانی (Fluidity) — همه‌ی گذارها انیمیشنی شدند
- **ورود پلکانی کارت‌ها**: هر کارت با تاخیر ۲۸ms و fade+rise نرم ظاهر می‌شود (سقف ۴۵۰ms)
- **hover کارت‌ها**: بالا آمدن + گلو + بوردر، همه با انیمیشن ۱۵۰-۲۱۰ms — دیگر هیچ پرش آنی وجود ندارد
- **میکروتعامل دکمه‌ها**: فشردن هر دکمه با scale نرم ۰/۹۷ (قالب سراسری — همه‌ی پنجره‌ها)
- **دکمه‌های سایدبار**: اکسنت‌بار ایریدسنت + تینت چیپ آیکون با گذار انیمیشنی
- **اعلان‌های Toast شیشه‌ای**: جای MessageBox های موفقیت — ورود slide+fade، خروج خودکار پس از ۲/۸ ثانیه
- **پس‌زمینه زنده**: تنفس آهسته سحابی‌ها (۱۳ و ۱۷ ثانیه) + BitmapCache برای رندر GPU
- **نبض هاله‌ی لوگو** + **شنای آیکون خالی‌استیت** + **خط مویی گرادیانی زیر تایتل‌بار**
- متن خالی‌استیت به‌روزشد: «سایت رقبا را اسکن کنید یا اولین محصول را اضافه کنید...»

نکته پرفورمنس: همه‌ی انیمیشن‌های دائمی روی Opacity/Transform سبک‌اند و سحابی‌ها با BitmapCache روی GPU رندر می‌شوند — افت فریمی وجود ندارد.

## ✨ نسخه ۲.۵.۳ — تغییر نام پروژه + حذف دکمه Import

### ۱) نام جدید پروژه: «بازارسنج | BazarSanj»
نام انتخابی برای فاز جدید: **رصد و سنجش قیمت رقبای بازار**.
- تیتر پنجره: «بازارسنج | BazarSanj»
- برند تایتل‌بار: BAZARSANJ + تگ‌لاین «هوش قیمت‌گذاری و رصد رقبا»
- متادیتای اسمبلی (AssemblyTitle/Product): BazarSanj
- UserAgent موتور بروزرسانی و اسکریپت آپدیت: BazarSanj / بازارسنج
- README بازنویسی شد
- ⚠️ namespace کد (`TorobScanner`) دست‌نخورده ماند تا سازگاری کامل با دیتابیس و آپدیت حفظ شود

### ۲) حذف دکمه «وارد کردن لینک» از سایدبار
دکمه Import لینک از txt/csv حذف شد (روش اصلی افزودن محصول: اسکن سایت‌های رقیب).
سرویس Export اکسل و بقیه قابلیت‌ها دست‌نخورده‌اند.

## 🔥🔥 هات‌فیکس ۲.۵.۲ — علت اصلی کرش پیدا شد: «Transform is not valid for Window»

با گارد استارت‌آپ ۲.۵.۱، پیام واقعی خطا نمایان شد:
**`Transform is not valid for Window.`** — این همان علت پشت‌صحنه‌ی کرش اول بود.

### باگ ۲۰ — انیمیشن ورود روی خود Window ممنوع است (علت ریشه‌ای کرش استارتاپ)
**فایل:** `Views/ThemeHelper.cs`

**مشکل:** در WPF، کلاس `Window` به‌دلیل اینکه یک HWND واقعی سیستم‌عامل است، setter
خصوصیت `RenderTransform` آن هر مقداری غیر از همانی (Identity) را با
`InvalidOperationException: Transform is not valid for Window` رد می‌کند.
کد انیمیشن ورود ما در `ApplyObsidianTheme` می‌نوشت:
`window.RenderTransform = new TranslateTransform(0, 10);`
→ همین خط در سازنده‌ی همه‌ی پنجره‌ها استثنا می‌داد → کرش استارتاپِ نسخه ۲.۵.۰.

**راه‌حل:** انیمیشن ورود دو بخش شد:
- **Fade** (شفافیت ۰→۱) روی خود پنجره — روی Window کاملاً مجاز است
- **Rise** (حرکت ۱۲px رو به بالا) روی **محتوای ریشه‌ی پنجره** (Border/ Grid داخلی) —
  روی UIElement معمولی RenderTransform مجاز است و همان جلوه بصری قبلی حفظ شد

این رفع باگ روی همه‌ی ۶ پنجره (اصلی + ۵ دیالوگ) اعمال می‌شود چون همه از همین
تابع استفاده می‌کنند.

### نکته برای اجرای صحیح
برنامه را از مسیر **publish** یا بیلد Release اجرا کنید؛ اجرا از `bin\Debug` هم
بعد از این رفع باگ سالم است، ولی برای استفاده‌ی روزمره خروجی Release توصیه می‌شود.

---

## 🔥 هات‌فیکس ۲.۵.۱ — خطای «The invocation of the constructor ... threw an exception»

اگر بعد از اجرا پیام خطایی شبیه اسکرین‌شات می‌دیدید، این هات‌فیکس برای همان است.
سه مشکل واقعی پیدا و رفع شد + یک مقاوم‌سازی محیطی:

### باگ ۱۷ — پیام خطا «علت واقعی» را نشان نمی‌داد + پروسه‌ی شبح
**فایل:** `App.xaml.cs`

**مشکل:** وقتی استثنا از داخل سازنده‌ی `MainWindow` بالا می‌آمد، WPF آن را در یک
`XamlParseException` می‌پیچد. هندلر قبلی فقط `e.Exception.Message` (لایه بیرونی) را
نشان می‌داد؛ یعنی کاربر پیام بی‌فایده‌ی «The invocation of the constructor on type
'TorobScanner.MainWindow' ... threw an exception» را می‌دید و **علت اصلی** فقط داخل
error_log.txt می‌ماند. بدتر اینکه چون `e.Handled = true` می‌شد و هنوز هیچ پنجره‌ای
ساخته نشده بود، برنامه به‌صورت **پروسه‌ی شبح بدون پنجره** زنده می‌ماند: کاربر OK را
می‌زد و هیچ اتفاقی نمی‌افتاد — نه پنجره، نه بستن.

**راه‌حل:**
- تابع `RootMessage`: زنجیره‌ی `InnerException` تا ۱۰ لایه باز می‌شود و گویاترین پیام (علت واقعی) نمایش داده می‌شود.
- اگر هنگام کرش هیچ پنجره‌ی قابل‌مشاهده‌ای باز نباشد، برنامه با پیام مناسب **تمیز بسته می‌شود** (دیگر پروسه‌ی شبح وجود ندارد).
- **مسیر کامل** `error_log.txt` داخل خود پیام نمایش داده می‌شود تا کاربر بتواند فایل را پیدا و ارسال کند.
- راهنمای سریع (دو علت رایج: کپی فقط exe و پوشه‌ی محافظت‌شده) داخل پیام آمده است.

### باگ ۱۸ — مقاوم‌سازی دیتابیس در برابر محیط‌های مشکل‌دار
**فایل:** `Data/DatabaseManager.cs`

**مشکل:** دو سناریوی رایجِ محیطی باعث همان کرش استارتاپ می‌شد:
۱) **نبودن `e_sqlite3.dll`** کنار exe (مثلاً فقط فایل exe کپی شده بود یا exe جدید داخل
پوشه‌ی قدیمی گذاشته شده بود) → `DllNotFoundException` انگلیسیِ نامفهوم.
۲) **پوشه‌ی برنامه قابل نوشتن نبود** (Program Files، درایوهای محافظت‌شده، قفل آنتی‌ویروس)
→ `SqliteException: unable to open / readonly` در اولین `connection.Open()`.

**راه‌حل:**
- پیش از باز کردن دیتابیس، **قابلیت نوشتن پوشه** با یک فایل آزمایشی موقت تست می‌شود؛
  اگر شکست خورد، دیتابیس خودکار به `%APPDATA%\TorobScanner\` منتقل و **دیتابیس قبلی
  کاربر به مکان جدید کپی می‌شود** (هیچ داده‌ای از دست نمی‌رود).
- `DllNotFoundException` مربوط به SQLitePCLRaw گرفته می‌شود و پیام فارسیِ گام‌به‌گام
  نمایش داده می‌شود: «کل محتویات ZIP را استخراج کنید — نه فقط exe».
- `SqliteException` هم با سه راه‌حل پیشنهادی (خروج از Program Files، خروج از OneDrive،
  بررسی آنتی‌ویروس) تبدیل به پیام فارسی می‌شود.

### باگ ۱۹ — گارد استارت‌آپ در سازنده MainWindow
**فایل:** `MainWindow.xaml.cs`

**مشکل:** کل بدنه‌ی سازنده (دیتابیس + ساخت UI + بارگذاری محصولات) بدون محافظ بود؛
هر استثنایی مستقیم به XamlParseException انگلیسی تبدیل می‌شد.

**راه‌حل:** کل راه‌اندازی داخل `try/catch` با لاگ‌برداری کامل است. حالا در بدترین حالت
کاربر پیام فارسی «خطای راه‌اندازی» را با **علت واقعی + دو راه‌حل رایج + مسیر فایل لاگ**
می‌بیند و برنامه تمیز بسته می‌شود.

---

## بخش اول: باگ‌های رفع‌شده در ۲.۵.۰ 🐛

### باگ ۱۳ — قفل دیتابیس هنگام اسکن همزمان (بحرانی)
**فایل:** `Data/DatabaseManager.cs`

**مشکل:** رشته اتصال از `Cache = SqliteCacheMode.Shared` استفاده می‌کرد در حالی که
موتور بروزرسانی (`TorobProductScraper`) با سیمافور ۳ تایی، **۳ نویسنده همزمان** روی
دیتابیس دارد. در حالت Shared Cache، قفل‌ها به سطح جدول می‌روند و خطای
`SQLITE_LOCKED (database table is locked)` دقیقاً وقتی رخ می‌دهد که دو کانکشن
همزمان بخواهند بنویسند — و نکته خطرناک اینجاست که **busy-timeout این خطای خاص را
تکرار نمی‌کند**؛ یعنی کاربر وسط اسکن با خطای تصادفی «Save ناموفق» مواجه می‌شد
(لاگ‌ها فقط `Logger.Error` می‌شدند و محصول آپدیت نمی‌شد).

**راه‌حل:** Shared Cache حذف شد؛ جایگزین: `Pooling=true` + `DefaultTimeout=30` (ثانیه).
با WAL که از قبل فعال بود، نوشتن‌ها به‌صورت صف خودکار و امن سریالایز می‌شوند و
Pooling هزینه‌ی باز/بسته شدن کانکشن‌ها را هم حذف می‌کند.

### باگ ۱۴ — اسکن چندصفحه‌ای سایت‌های «بدون لینک صفحه‌بندی» بعد از صفحه ۲ می‌شکست
**فایل:** `Scrapers/ExtractorScripts.cs` (تابع `findNext`)

**مشکل:** برای سایت‌هایی که فلگ `ForceTryPagination` دارند (کافه‌کالا، کافئین‌اکسپرس)،
وقتی موتور به صفحه ۲ می‌رسید و باز هم لینک «بعدی» پیدا نمی‌کرد، دوباره مسیر
`/page/2/` را **به انتهای URL اضافه می‌کرد** و آدرس‌هایی مثل
`/category/coffee/page/2/page/2/` ساخته می‌شد → خطای 404 → پایان اسکن.
یعنی عملاً فقط ۲ صفحه از این سایت‌ها اسکن می‌شد.

**راه‌حل:** ابتدا شماره صفحه فعلی از مسیر URL خوانده می‌شود (`/page/N/`) و اگر
وجود داشت، `/page/N+1/` ساخته می‌شود. فقط برای صفحه ۱ است که `/page/2/` می‌سازد.

### باگ ۱۵ — رقابت (Race Condition) شمارنده پیشرفت
**فایل:** `Scrapers/TorobProductScraper.cs`

**مشکل:** شمارنده `current` با `Interlocked.Increment` زیاد می‌شد اما **بدون خواندن
اتمی** مستقیم به `progress.Report` پاس می‌داد. با ۳ تسک موازی، عدد نمایش‌داده‌شده
گاهی تکراری یا جاافتاده بود و نوار پیشرفت پرش‌های عجیب می‌زد.

**راه‌حل:** نتیجه Increment در متغیر محلی خوانده می‌شود (`int done = Interlocked.Increment(ref current)`)
و در نقاط دیگر از `Volatile.Read` استفاده شد.

### باگ ۱۶ — ثبت دسته‌بندی با فاصله‌های اضافه
**فایل:** `Views/ImportSettingsWindow.cs`

**مشکل:** نام دسته جدید بدون `Trim` ثبت می‌شد؛ «قوه␣␣» با «قوه» یکی تلقی نمی‌شد و
دسته‌های تکراری موازی ساخته می‌شد (هم در فرم Import و هم در پنل مدیریت — هر دو اصلاح شد).

---

## بخش دوم: محدودیت UI که حل شد 🪟

### پنجره‌های borderless اصلاً تغییر اندازه نداشتند
**فایل‌ها:** `MainWindow.xaml.cs`، `Views/ManagementWindow.cs`

**مشکل:** پنجره اصلی با `WindowStyle=None` ساخته شده بود اما هیچ مکانیزمی برای
تغییر اندازه، حداکثر کردن یا Aero Snap نداشت — کاربر با هر اندازه صفحه‌ای که داشت
ژست ثابت 1280×820 را می‌دید.

**راه‌حل:** `WindowChrome` ویندوزی متصل شد (CaptionHeight=54، ResizeBorderThickness=10):
- تغییر اندازه از لبه‌ها و گوشه‌ها ✅
- دکمه حداکثر/بازگردانی + دابل‌کلیک روی نوار عنوان ✅
- Snap به لبه‌های صفحه (Win+جهت‌ها) ✅
- در حالت حداکثر، گوشه‌های گرد و سایه به‌صورت خودکار صاف می‌شوند ✅

---

## بخش سوم: بازطراحی کامل ظاهر — تم Platinum Glass ✨

**فایل‌ها:** `Themes/LuxTheme.xaml` (جدید)، `Views/LuxUI.cs` (جدید)، `Views/ThemeHelper.cs`
و بازنویسی کامل همه پنجره‌ها.

سبک انتخابی: **گلس‌مورفیسم پلاتینیومی** (به سبک ترندهای ۲۰۲۵ — Apple Vision Pro)
روی پس‌زمینه‌ی ابیس با لهجه‌های نقره‌ای و گرادیان‌های ایریدسنت نرم.

### چه چیزهایی تغییر کرد:

| بخش | قبلاً | حالا |
|------|-------|------|
| تم | «Obsidian» با نئون سیان/بنفش پررنگ (سایه‌های نئونی روی همه کارت‌ها) | شیشه‌های مات چندلایه + سحابی‌های محو + پلاتینیوم متالیک |
| فونت | Segoe UI | **Vazirmatn باندل‌شده** (۵ وزن داخل exe جاسازی می‌شود) |
| اعداد | لاتین | **فارسی** (۰۱۲۳...) + زمان نسبی «۲ ساعت پیش» روی کارت‌ها |
| استایل کنترل‌ها | فقط ComboBox تم داشت؛ بقیه دکمه‌ها با کروم پیش‌فرض ویندوز! | تم سراسری implicit برای **همه‌چیز**: دکمه، TextBox، ComboBox، ListBox، DataGrid، ScrollBar باریک، ToolTip، Separator، ProgressBar |
| نوار پیشرفت | مستطیل ساده | نوار شیشه‌ای گرد با گرادیان ایریدسنت + انیمیشن indeterminate |
| آمار | ۳ کارت ثابت 200px | کارت‌های بنتو واکنش‌گرا با چیپ آیکون و متن گرادیانی |
| کارت محصول | نئون سبز خام | نوار لهجه ایریدسنت + حباب‌های تغییر قیمت رنگی + hover نرم (lift + glow فقط هنگام hover) |
| لیست خالی | هیچی (سفید خالی) | Empty state با دکمه «افزودن اولین محصول» |
| دیالوگ‌ها | هرکدام طرح خودشان | پوسته یکدست: چیپ آیکون + عنوان + دکمه بستن + دکمه اصلی پلاتینیومی |
| پنل مدیریت | پنجره معمولی ویندوز | همان پوسته شیشه‌ای + resizable |
| تغییر اندازه | ❌ ممکن نبود | ✅ لبه‌ها + حداکثر + Snap |
| انیمیشن‌ها | Blink نقطه وضعیت | ورود نرم پنجره‌ها (۲۰۰ms) + hover شیشه‌ای دکمه‌ها + lift کارت‌ها — «متعادل» طبق انتخاب کاربر |

### نکات پرفورمنس (طبق درخواست «سنگین نشه»):
- سایه‌ی ثابت روی تک‌تک کارت‌های محصول **حذف شد** — گلو فقط هنگام hover ساخته می‌شود
  (در لیست‌های صدها ردیفی، DropShadowEffect ثابت = افت شدید FPS)
- افکت blur واقعی هیچ‌جا استفاده نشد؛ عمق شیشه با گرادیان‌های شبه‌مات ساخته شده
- سایه فقط روی ۱-۲ عنصر هر پنجره (قاب بیرونی + پاپ‌آپ ComboBox)
- انیمیشن‌ها کوتاه (۱۲۰-۲۳۰ms) و فقط opacity/transform (GPU-friendly)
- دیتاگرید مجهز به virtualization ردیف/ستون شد
- فونت‌ها Resource داخل exe هستند — بدون فایل کنار برنامه

### ساختار فایل‌های تم:
```
Themes/LuxTheme.xaml   ← دیکشنری کامپایل‌شده (BAML) — بالای فایل پالت رنگ است؛
                          برای تغییر طبع بصری فقط همان چند خط را عوض کنید
Views/LuxUI.cs         ← کارخانجاهای UI مشترک (پوسته دیالوگ، pill، چیپ، اعداد فارسی)
```

---

## بخش چهارم: موارد نگهداری 🔧

- `TorobScanner.csproj`: نسخه → **2.5.0** (موتور آپدیت خودکار با تگ `v2.5.0` فعال می‌شود) + ۵ فونت Resource + `NeutralLanguage=fa-IR`
- `README.md`: بازنویسی کامل برای v2.5
- هیچ تغییری در منطق اسکرپینگ، دیتابیس (به‌جز باگ ۱۳)، Import/Export و آپدیت‌سرور داده نشد
- دیتابیس‌های قدیمی کاربران بدون هیچ مهاجرتی کار می‌کنند (اسکیمای جدول دست نخورده)
- بیلد در CI لینوکس تست شد: `dotnet publish -r win-x64 --self-contained false` ✅ (همان pipeline مربوط به release.yml)
- فونت‌ها و تم در خروجی publish تأیید شدند (embed شده در TorobScanner.dll)

## ✅ چک‌لیست صحت
- [x] `dotnet build -c Release` → 0 خطا، 0 هشدار
- [x] `dotnet publish -c Release -r win-x64 --self-contained false` → موفق
- [x] فونت‌های Vazirmatn داخل اسمبلی embed شده‌اند (`fonts/*.ttf` در g.resources)
- [x] تم کامپایل‌شده (`themes/luxtheme.baml`) داخل اسمبلی موجود است
- [x] نسخه 2.5.0 در متادیتا اسمبلی
